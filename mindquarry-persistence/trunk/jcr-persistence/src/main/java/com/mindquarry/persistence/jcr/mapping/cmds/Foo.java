package com.mindquarry.persistence.jcr.mapping.cmds;

public class Foo {

    /*
     * transformer = model_.entityTransformer(entity.class());
     * transformer.writeToJcr(entity, session);
     * 
     * 
     * 
     * 
     * 
     * 
     * transformer = model_.entityTransformer();
     * entity = transformer.readFromJcr(jcrNode);
     */
}
